[build]
  # Build the Storyblok plugin from the monorepo using Netlify's Next.js runtime.
  base = "."
  command = "pnpm --filter @content-guard/storyblok-plugin build"

[build.environment]
  NODE_VERSION = "22"
  PNPM_VERSION = "11.9.0"
