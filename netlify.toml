[build]
  # Build the Storyblok plugin from the monorepo and keep publish output separate from base.
  base = "."
  command = "pnpm --filter @content-guard/storyblok-plugin build"
  publish = "apps/storyblok-plugin/.next"

[build.environment]
  NODE_VERSION = "22"
  PNPM_VERSION = "11.9.0"
