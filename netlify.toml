[build]
  # Build the Storyblok plugin from its subdirectory using Netlify's Next.js runtime.
  base = "apps/storyblok-plugin"
  command = "pnpm build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
  PNPM_VERSION = "11.9.0"
